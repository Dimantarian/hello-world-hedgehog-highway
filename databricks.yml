bundle:
  name: hedgehog-highway

include:
  - resources/*.yml

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: ${var.databricks_host}

variables:
  databricks_host:
    description: Databricks workspace URL
    default: https://fevm-serverless-j71s4d.cloud.databricks.com
